/// @description Insert description here
// You can write your code in this editor

draw_sprite_ext(
	sprite_index,
	image_index,
	x,
	y-z,
	image_xscale,
	image_yscale,
	image_angle,
	image_blend,
	image_alpha
	)
	//draw_text(MEDIOX,MEDIOY,string(state))
	//draw_text(MEDIOX,MEDIOY+16,string(lengthdir_x(GridSpeed,InputDirection)))
	//draw_text(MEDIOX,MEDIOY+32,string(lengthdir_y(GridSpeed,InputDirection)))
	//draw_text(MEDIOX,MEDIOY+32+16,string(x))
	//draw_text(MEDIOX,MEDIOY+64,string(y))